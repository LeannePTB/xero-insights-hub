# Roadmap

- [ ] Replace role-check authorisation with access checks on write paths (plan in .lovable/plan.md awaiting approval; five open questions)
- [x] Phase 2 — Guardrails, part 1: access matrix (source of truth + generated document), aal1 allow-list, verified admin-client register, static guard tests, `security_test_runs` + `record_access_test_run()`, `access_tests` posture check, tightened `definer_guards`, `bun run security:check`
- [x] Phase 2 — Guardrails, part 2: `security_test_runs` grant fix, `excess_grants` posture check, PGlite fixture auth mirror (auth.uid/jwt/users/mfa_factors, table + column grants, BYPASSRLS definer owners), catalogue fingerprint check, matrix-driven PGlite suite (1,212 rows proved) and the three meta tests
- [x] Phase 2 — part 2 review corrections (Part A): `firms` update, `client_subscriptions` writes and unaudited Path C writes reverted to known failures (backlog 27, 28, 29); `firm_members` read recorded as an owner-approved Path C item
- [x] Phase 2 — part B: close the `firms` ownership escalation (revoke write grants, drop `super_admin updates firms`, `set_firm_always_free` pinned to the practice organisation, re-target `firms` and `signup_requests` policies to `authenticated`, audit row on logo clear, `always_free` posture check)
- [ ] DEFERRED (owner decision, 11 Sep 2026): Phase 2 — Guardrails, part 3 is PARKED until after Phase 4, to be re-scoped as a slim smoke test with no super-admin test account. Original scope: live smoke suite (ZZ Security Test Org, per-role test accounts with TOTP secrets in project secrets, server-function calls, cleanup, exclusion from posture counts) and the "Run access tests" button on /admin/security. `bun run security:check` must also call `public.security_posture()` through the security-runner aal2 session and fail on any unexpected Action (the sandbox role cannot execute it today).

- [x] Fix the firm-subscription super_admin bypass (`resolveAccess`/`assertAccess`)
- [x] Add Allyce and Chantelle as staff of Autotek NSW and Bangkok On Darby (Path A membership)
- [x] Phase 1 — enforce MFA (aal2) on the server: middleware wrapper, restrictive policies on all 49 in-scope tables, guards on 26 definer functions
- [x] Phase 1b — Security posture card in the left sidebar (live checks, online chips, shared with /admin/security)
- [x] Phase 1b corrections — presence for everyone, server-set last active, restored token/PKCE/TLS/HIBP checks, requested card design
- [x] Security posture follow-up — verified names, profile column grants, presence grants, and audit check
- [x] Phase 3a (done 11 Sep 2026) — support grants are read-only everywhere: split `user_can_manage_client` into read/write helpers, database write checks for server-function writes (backlog 18 + 32), audit row on client logo clear, support read on `clients`/`client_statutory_accounts` (backlog 25), rewrite the `support_write` posture check, matrix updates
- [x] Phase 3b (done 11 Sep 2026) — super-admin powers bounded and audited: bounded self-join RPC (backlog 30), `client_subscriptions` writes locked down behind audited aal2 functions (backlog 28), one generic audit trigger on the seven Path C tables (backlog 29), fail-closed always-free with a required reason and posture Action (backlog 31), fixture now carries triggers
- [x] Phase 4 batch 1 (one rulebook — Xero read gate), 11 Sep 2026: dashboard gate, tenant-ownership proof and scenario/consolidation/reconciliation/audit authorisation moved into caller-scoped aal2 database functions; per-batch static guard 6 with `docs/security/converted-files.ts`. Batches 2-5 outstanding.
- [x] Phase 4 batch 2 (one rulebook — client data reads and writes), 11 Sep 2026: client, client-access, loan consolidation and consolidation group authorisation moved into caller-scoped aal2 database functions; converted-file guard extended. Batches 3-5 outstanding.
- [x] Phase 4 batch 3 (one rulebook — super admin / Path C), 11 Sep 2026: one database super-admin and advisor check replaces nine TypeScript copies; advisor role changes, organisation member lists (with verified auth emails) and plan usage counts moved into caller-scoped aal2 database functions. Batches 4-5 outstanding.
- [x] Phase 4 batch 4 (one rulebook — reports, email and branding), 11 Sep 2026: report, report-link, email queue and branding paths classified and registered; the last three report `profiles.email` fallbacks removed; direct aal2 guards added to the three batch 3 functions.
- [x] Phase 4 COMPLETE — batch 5 (leftovers), 11 Sep 2026: every remaining `supabaseAdmin` site in `src/` is a registered system context or authorises through a caller-scoped aal2 database function first. Five new caller-scoped RPCs (`my_roles`, `my_firm_memberships`, `my_client_access`, `firm_support_grants`, `firm_support_viewer_state`); "Sign in with Xero" and the sign-in receipt split into their own system-context files; the Batch 2 guard exemptions removed; backlog 21, 22 and 23 closed. Next: Phase 5.

- [ ] DEFERRED (owner decision, 11 Sep 2026): People-and-access redesign — standing client-viewer grants and owner-managed invites. Design recorded in `docs/design/people-and-access.md`; build only after Phases 4–7 are complete.

- [x] Phase 5 steps 1-4 (Xero connection lifecycle), 12 Sep 2026: disconnect marks the row instead of deleting it (keeping the `client_xero_orgs` link, which the `ON DELETE CASCADE` was destroying), authorisation moved into `public.user_can_disconnect_xero_connection`, reconnect refreshes only the reconnecting person's row, only a definitive `invalid_grant` marks a grant revoked, and disconnected files no longer consume a client's Xero file allowance (`app_private.client_xero_files_used`).
- [x] Phase 5 step 5 (orphan prevention), 12 Sep 2026: `xero_connections.firm_id` is `NOT NULL` with deferred constraint triggers keeping a Xero file in the same organisation as the client it is linked to; the connect callback refuses and reports a tenant it cannot place instead of storing it unassigned; unlinking keeps the organisation stamp. Disconnect dialog copy rewritten to match mark-not-delete behaviour. Phase 5 COMPLETE — next is Phase 6.
- [x] Phase 7 (tidy up and prove it), 12 Sep 2026 — Batch 1 grant trim (DONE), Batch 2 generated definer register (DONE — `docs/security/definer-register.md`, checked by `security:check`), Batch 3 documentation that matches reality (DONE), Batch 4 full re-audit (DONE, 12 Sep 2026, with the query or code line inline for every finding and every clean verdict). Backlog 38 closed the same day: every path that marks a Xero connection disconnected because the grant is dead now clears both encrypted token columns in the same update. Re-audit raised four new low-severity items, 40–43 (unsubscribe token/rate limit, EXECUTE for PUBLIC on three trigger functions, 46 policies with no explicit `TO` clause, eight legacy `FOR ALL` policies). Backlog 18's remnant (`app_private.user_can_manage_client` admitting super admin + active support grant on read paths) and 37 (legacy alias removal) remain open by owner decision. Not verifiable from the sandbox: anything needing an aal2 browser session — the posture card, admin screens and the owner-facing screen tests.
  - Owner decisions settled 12 Sep 2026: an organisation may NOT read its own audit log (current behaviour correct, recorded as a deliberate decision); the super admin without MFA is left as is and will enrol at next sign-in; the slim live smoke suite is approved at reduced scope AFTER Phase 7.
  - Surfaced by Batch 2/3 and still open: backlog 34 (`access_invites.token_hash` covered by table-level SELECT), 35 (unused `report_cache`), 36 (four definer functions with no caller found), 37 (legacy `user_can_access_*` aliases — their own change), 38 (revoked Xero token ciphertext kept until overwritten), 39 (assessment evidence gaps with no artefact: key and secret rotation, incident drill and register, retained dependency scans, platform attestations, tested restore, published security contact, periodic access review).
  - After Phase 7: the slim live smoke suite (no super-admin test account), then the deferred people-and-access redesign.
- [x] Phase 6 (audit the reading of client financial data), 12 Sep 2026: one writer records every read of a client's figures — live, stored snapshot, stored report and public report link — with a stable read key, source, period and access path, never any figures. Five-minute de-duplication per person and client; new `read_audit` posture check computed from the trail against figures actually served; static guard 7 catches drift; audit export split into security events and client data reads. Backlog item 4 closed. Next is Phase 7.
- [x] Final hygiene batch (backlog 40–43), 12 Sep 2026: EXECUTE revoked from PUBLIC/anon on the three `app_private` plan-limit trigger functions (triggers still fire; callable definers 97 → 94); unsubscribe tokens hashed at rest (`token_hash` added, existing row converted, `token` dropped) with IP rate limiting on both route methods; explicit `TO` on all 34 policies that had none (9 `service_role`, 25 `authenticated`); all 20 permissive `FOR ALL` policies split into per-command policies from the live catalogue (restrictive `mfa_aal2_required` guards untouched). Matrix identical at every step: 1288 rows, 1222 proved, 0 failed, 0 known failures; fixture fingerprint now `4b72d63d…`; 40 tests pass; linter unchanged at 74 warnings in the pre-existing signed-in SECURITY DEFINER class. Not verified here: anything needing an aal2 browser session (owner screen tests, posture card, a live unsubscribe click).
