# Plan: Default-cards tick boxes

## Goal
Respond to the request about default-cards tick boxes on the firm dashboard.

## Decision
Leave the current implementation as-is. No code changes, no UI edits, no database migrations.

## Reasoning
The user explicitly said "Leave as is" regarding the default-cards tick boxes. The current behavior remains unchanged.

## Next steps
- No action required.
- Revisit if the user later decides to change the default-cards behavior.
